<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Allocation_Basis__c</fullName>
    <externalId>false</externalId>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <inlineHelpText>How an expanded line divides its measures among its buckets. NONE (default) does not divide - only valid when there is no expansion, or when Suppress Amounts is set. EVEN divides equally across the buckets a line occupies. Quantities are handled separately: they repeat rather than divide on any axis where the same units are present each period.</inlineHelpText>
    <label>Allocation Basis</label>
    <length>20</length>
    <required>false</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
