<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Partition_Value__c</fullName>
    <externalId>false</externalId>
    <inlineHelpText>Which partition this table is - the scenario, entity or department it covers. Blank on an unpartitioned table. Carried as its own field rather than only inside Table_Key__c because a composite key is an identity, not an API: a renderer should not have to parse one.</inlineHelpText>
    <label>Partition Value</label>
    <length>80</length>
    <required>false</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
