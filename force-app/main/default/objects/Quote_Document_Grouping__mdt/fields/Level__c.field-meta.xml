<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Level__c</fullName>
    <externalId>false</externalId>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <inlineHelpText>Nesting depth, outermost is 1. Two groupings on DIFFERENT levels nest - Phase then Family. Two groupings on the SAME level combine into one composite bucket - Hardware / Recurring as a single group rather than one inside the other. Sequence orders the parts within a level.</inlineHelpText>
    <label>Level</label>
    <precision>2</precision>
    <required>true</required>
    <scale>0</scale>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
