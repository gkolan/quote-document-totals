<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <deploymentStatus>Deployed</deploymentStatus>
    <description>One column of a generated table: which field it binds, in what order, under what heading, formatted as what type. Before this object existed the column layout lived only in a Word template's Value Select tags, which is the single largest reason a second renderer was expensive - the layout had to be rebuilt by hand and could silently diverge. A renderer now reads columns rather than being configured with them.</description>
    <enableActivities>false</enableActivities>
    <enableHistory>false</enableHistory>
    <enableReports>true</enableReports>
    <enableSearch>true</enableSearch>
    <label>Quote Document Column</label>
    <nameField>
        <displayFormat>QDC-{0000000000}</displayFormat>
        <label>Column Number</label>
        <type>AutoNumber</type>
    </nameField>
    <pluralLabel>Quote Document Columns</pluralLabel>
    <sharingModel>ControlledByParent</sharingModel>
    <visibility>Public</visibility>
</CustomObject>
