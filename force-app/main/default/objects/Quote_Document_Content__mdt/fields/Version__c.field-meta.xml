<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Version__c</fullName>
    <externalId>false</externalId>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <inlineHelpText>Bump whenever the wording changes. Snapshotted onto every generated block as Source Version, and hashed into the fingerprint, so changing a clause regenerates rather than silently leaving old documents claiming the new text. Deploying this record does NOT by itself mark quotes stale - run the step 05 invalidation job.</inlineHelpText>
    <label>Version</label>
    <length>40</length>
    <required>true</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
